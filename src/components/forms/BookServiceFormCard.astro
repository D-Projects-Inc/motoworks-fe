<div class="card">
  <!-- Title x Form Text-->
  <div class="card-body text-center pt-5">
    <legend class="display-6 mb-3 text-center">
      <strong>Book Your Motorcycle Service Appointment</strong>
    </legend>
    <p class="card-text text-center lead px-5">
      Please fill out the form below to book your motorcycle service appointment. 
      Our team will get back to you promptly to confirm your booking and provide 
      any additional information you may need.
    </p>
  </div>
  <center><hr class="my-4 w-50"></center>

  <!-- Form -->
  <p class="text-info text-center">
    We'll never share your personal details with anyone else.
  </p>
  <form class="p-4 pt-3" id="BookServiceForm">
    <div class="card-body">
      <!-- Customer Information -->
      <div class="row g-2 mb-5">
        <!-- Full Name -->
        <div class="col-auto">
          <div class="form-floating">
            <input name="fullname" 
              maxlength="50" type="text" class="form-control" 
              id="fullname" placeholder="Enter your first and last name" 
              required
            >
            <label for="fullname" class="form-label">Full Name *</label>
          </div>
        </div>
        <!-- Email -->
        <div class="col-auto">
          <div class="form-floating">
            <input type="email" class="form-control" 
              id="email_address" name="email_address" 
              placeholder="We'll send your booking confirmation here"
              required
            >
            <label for="email_address" class="form-label">Email Address *</label>
          </div>
        </div>
        <!-- Phone Number -->
        <div class="col-auto">
          <div class="form-floating">
            <input type="tel" 
              class="form-control" 
              id="phone_number" 
              name="phone_number" 
              placeholder="In case we need to contact you about your booking."
              required
            >
            <label for="phone_number" class="form-label">Phone Number *</label>
          </div>
        </div>
      </div>

      <!--[ Equipment/Vehicle Information ]-->
      <div class="row g-2 mb-5">
        <!-- Vehicle/Equipment Type -->
        <div class="col-auto">
          <div class="form-floating">
          <select class="form-select" id="equipmentType" name="equipment_type" required>
            <option value="">Select equipment...</option>
            <option>Motorcycle</option>
            <option>Generator</option>
            <option>Lawn Equipment</option>
            <option>Other</option>
          </select>
          <label for="equipmentType" class="form-label">Vehicle/Equipment Type *</label>
          </div>
        </div>
        <!-- vehicle_registration -->
        <div class="col-auto">
          <div class="form-floating">
            <input type="text" class="form-control" 
              id="vehicle_registration" name="vehicle_registration" 
              placeholder="Vehicle Make & Model (e.g., Honda CBR600RR)" 
              required
            >
            <label for="vehicle_registration">Vehicle Registration</label>
          </div>
        </div>
        <!-- Service Type -->
        <div class="col-auto">
          <div class="form-floating">
            <select class="form-select" id="serviceType" name="service_type" required>
              <option value="">Select service...</option>
              <option>Maintenance</option>
              <option>Repair</option>
              <option>Custom Job</option>
            </select>
            <label for="serviceType" class="form-label">Service Type *</label>
          </div>
        </div>
        <!-- Service Date & Time -->
        <div class="col-auto">
          <div class="form-floating">
            <input type="datetime-local" class="form-control" id="scheduled_date" name="scheduled_date" required>
            <label for="scheduled_date" class="form-label">Service Date & Time *</label>
          </div>
        </div>
      </div>

      <!--[ Service Details ]-->
      <div class="row g-2 mb-3">
        <!-- Problem Description -->
        <div class="col-12">
          <label for="notes" class="form-label">Problem Description / Notes (Optional)</label>
          <textarea class="form-control" id="notes" name="notes" rows="4" placeholder="Tell us what’s wrong or any special instructions"></textarea>
        </div>
      </div>

      <!--[ Submit Button ]-->
      <p class="">
        <input class="btn btn-success fw-bold mt-1 w-100" type="submit" value="Confirm Booking">
      </p>
    </div>
  </form>

  <!-- Form Footer -->
  <div class="card-footer text-muted text-center">
    <div class="text-secondary">
      <small>
        * Required fields
      </small>
    </div>

    <div>
      <p>
        “Once you submit, you'll receive a confirmation email with your booking details.”
        “Need help? Contact our support team at [support@moto-works.co.za].”
        Optional small note: “You don’t need an account to book, but logging in makes repeat bookings faster.”
      </p>
    </div>
  </div>
</div>